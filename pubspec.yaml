name: bgmi
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  
  # Firebase Packages
  firebase_core: ^2.32.0
  firebase_database: ^10.5.7
  firebase_auth: ^4.17.9   # NAYA: Login aur Register ke liye
  
  # Payment Package
  upi_india: ^3.0.1        # NAYA: PhonePe, GPay, Paytm open karne ke liye

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
